# Phase2: コンテンツ生成 - n8nワークフロー詳細設計書（改訂版 v2.0）

**フェーズ**: Phase2 - コンテンツ生成（Week 3-6）
**目標**: note×SNS×LINE連携で22,000PV構築
**総工数**: 80U = 240🍅
**状態**: ⏳ Phase1完了後に着手可能
**作成日**: 2025-10-27
**バージョン**: 2.0（Phase1実装変更を完全反映）
**前バージョンとの差分**: Phase1でNotion単独管理に変更したため全面改訂

---

## ⚠️ 重要: Phase1実装変更の反映

**Phase1での主要な変更**:
1. ✅ WF3 (n8n Data Pipeline) 廃止 → Notion単独管理
2. ✅ Google Sheets使用を全面廃止
3. ✅ note記事管理DB プロパティ変更・追加
4. ✅ Database ID確定: `29968d5c-2986-81ad-90d0-c24ed710503e`

**Phase2への影響**:
- すべてのデータ取得・保存が **Notionのみ** で完結
- プロパティ名の統一（Description, Image URL, Generated By, Author）
- Google Sheets API呼び出しを全削除
- データ統合・同期処理が不要

**詳細**: [[Phase1_実装変更点サマリー_Phase2への影響分析]]

---

## 📋 目次

1. [Phase2概要](#phase2概要)
2. [データベース設計](#データベース設計)
3. [WF5: トピック抽出AI](#wf5-トピック抽出ai)
4. [WF6: note記事自動生成](#wf6-note記事自動生成)
5. [WF7: SNS動画化](#wf7-sns動画化)
6. [WF8: SNS投稿自動化](#wf8-sns投稿自動化)
7. [WF9: LINE再配信](#wf9-line再配信)
8. [技術要件](#技術要件)
9. [テスト計画](#テスト計画)

---

## Phase2概要

### ビジネス目標

**定量目標**:
- note記事40本公開（週10本×4週）
- SNS投稿40本配信（週10本×4週）
- 累計PV 22,000達成

**定性目標**:
- AI自動生成による記事品質の一定化
- SNS×note×LINEのオムニチャネル配信
- トピックデータの蓄積とフィードバックループ構築

### 全体アーキテクチャ（Phase1変更反映版）

```
[Phase1完了]
   ↓
[Notion: note記事管理DB]
   ↓ Database ID: 29968d5c-2986-81ad-90d0-c24ed710503e
   ↓ Phase1のWF4で記事データ蓄積済み
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[WF5: トピック抽出AI] ← 人気トピック特定
   ↓ Notion Queryで記事取得（Google Sheets不使用）
[Notion: トピックマスタ] (新規作成)
   ↓ Relation: note記事管理DB
[WF6: note記事自動生成] ← GPT-4で記事作成
   ↓ Generated By = "AI" で登録
[Notion: note記事管理DB更新] + [note API投稿]
   ↓
┌──────────────┬──────────────┬──────────────┐
│              │              │              │
[WF7: SNS動画化] [WF8: SNS投稿] [WF9: LINE再配信]
   ↓              ↓              ↓
[Notion: 動画DB] [Notion: 投稿ログ] [Notion: LINE CRM]
```

### ワークフロー一覧

| WF | 名称 | 工数 | 優先度 | 依存 | 主要技術 | データソース |
|----|------|------|--------|------|----------|------------|
| **WF5** | トピック抽出AI | 18🍅 | P0 | WF4 | GPT-4, Notion API | Notion のみ |
| **WF6** | note記事自動生成 | 30🍅 | P0 | WF5 | GPT-4, note API | Notion のみ |
| **WF7** | SNS動画化 | 30🍅 | P1 | WF6 | Runway, Notion API | Notion のみ |
| **WF8** | SNS投稿自動化 | 24🍅 | P0 | WF6, WF7 | X API, Instagram API | Notion のみ |
| **WF9** | LINE再配信 | 18🍅 | P1 | WF6 | LINE API, Notion API | Notion のみ |

---

## データベース設計

### 既存Database: note記事管理DB（Phase1で作成済み）

**Database ID**: `29968d5c-2986-81ad-90d0-c24ed710503e`
**URL**: https://www.notion.so/29968d5c298681ad90d0c24ed710503e
**作成日**: 2025-10-27（Phase1-WF4で作成）

**プロパティ一覧（12個）**:

```yaml
Title (title):
  説明: 記事タイトル
  用途: 主キー、SNS投稿文生成

URL (url):
  説明: note記事URL
  用途: 記事リンク、SNS投稿

Article ID (rich_text):
  説明: note記事ID
  用途: 重複チェック、ユニークキー

Published Date (date):
  説明: 公開日時（ISO 8601形式）
  用途: ソート、フィルタリング

View Count (number):
  説明: 閲覧数
  用途: スコアリング（weight: 0.6）

Like Count (number):
  説明: いいね数
  用途: スコアリング（weight: 0.4）

Status (select):
  オプション:
    - 検知済み（緑）: WF4で検知直後
    - 分析済み（青）: WF5でトピック抽出完了
    - 配信済み（紫）: WF8/WF9で配信完了
    - アーカイブ（灰）: 古い記事・非アクティブ

Generated By (select):
  オプション:
    - 手動（デフォルト）: 手動で作成した記事
    - AI（ピンク）: WF6で自動生成した記事
  用途: AI生成記事と手動記事の区別

Description (rich_text):
  説明: 記事概要（OG:description）
  用途: SNS投稿文生成、トピック抽出

Image URL (url):
  説明: OG:image URL
  用途: SNS投稿画像、動画サムネイル

Author (rich_text):
  説明: 著者名（デフォルト: 61beef）
  用途: 複数ライター対応、署名

Category (multi_select):
  説明: カテゴリタグ（複数選択可）
  用途: トピック分類、フィルタリング
```

---

### 新規Database 1: トピックマスタ

**Database名**: `トピックマスタ`
**作成タイミング**: WF5実装前
**親ページ**: note記事管理DBと同じ親ページ推奨

**プロパティ設計**:

```yaml
Topic Name (title):
  説明: トピック名
  例: "MEO対策の基礎", "英語教育トレンド"

Score (number):
  説明: 人気度スコア
  計算式: (avg_view_count * 0.6) + (avg_like_count * 0.4)
  用途: WF6でトピック選択時の優先度

Keywords (multi_select):
  説明: 関連キーワード
  例: ["MEO", "Googleマップ", "集客"]
  用途: 記事生成時のキーワード指定

Related Articles (relation):
  説明: 関連記事リスト
  Relation先: note記事管理DB (29968d5c-2986-81ad-90d0-c24ed710503e)
  用途: トピックと記事の紐付け

Extracted At (date):
  説明: 抽出日時
  更新頻度: 週2回（水・土 09:00）

Status (select):
  オプション:
    - active（緑）: 現在のトレンド
    - archived（灰）: 過去のトピック
  用途: WF6でactiveトピックのみ使用
```

**作成用スクリプト**: Notion API または GUI で作成

---

### 新規Database 2: 動画管理DB

**Database名**: `動画管理DB`
**作成タイミング**: WF7実装前

**プロパティ設計**:

```yaml
Title (title):
  説明: 動画タイトル
  例: "MEO対策で集客力UP！3つのポイント"

Video URL (url):
  説明: Google Drive動画URL
  用途: WF8でSNS投稿時に使用

Related Article (relation):
  説明: 元記事
  Relation先: note記事管理DB (29968d5c-2986-81ad-90d0-c24ed710503e)

Platform (multi_select):
  オプション: ["YouTube", "Instagram", "TikTok"]
  用途: 投稿先プラットフォーム管理

Duration (number):
  説明: 動画長（秒）
  制約: 30-60秒推奨

Generated At (date):
  説明: 生成日時

Status (select):
  オプション:
    - pending（黄）: 生成待ち
    - generated（青）: 生成完了
    - published（緑）: 投稿完了
    - failed（赤）: 生成失敗
```

---

### 新規Database 3: SNS投稿ログ

**Database名**: `SNS投稿ログ`
**作成タイミング**: WF8実装前

**プロパティ設計**:

```yaml
Platform (select):
  オプション: ["X", "Instagram"]
  説明: 投稿先プラットフォーム

Post ID (text):
  説明: プラットフォーム投稿ID
  例: X "1234567890123456789", Instagram "18123456789"

Post URL (url):
  説明: 投稿URL
  用途: 投稿確認、エンゲージメント測定

Related Article (relation):
  説明: 元記事
  Relation先: note記事管理DB (29968d5c-2986-81ad-90d0-c24ed710503e)

Post Text (rich_text):
  説明: 投稿文
  用途: 投稿履歴、効果測定

Published At (date):
  説明: 投稿日時

Engagement (number):
  説明: エンゲージメント数
  計算: いいね + RT + リプライ（X）、いいね + コメント（Instagram）
  用途: 効果測定
```

---

## WF5: トピック抽出AI

### 基本情報

- **工数**: 18🍅（6ユニット）
- **目的**: note記事管理DBから人気記事を分析し、トレンドトピックを抽出
- **トリガー**: Schedule（週2回：水曜・土曜 09:00）
- **実行頻度**: 週2回
- **データソース**: **Notion のみ**（Google Sheets不使用）

### 設計意図

**なぜこのワークフローが必要か**:
- 手動でトピック選定すると属人的になる
- データ駆動でトピック決定することで、PV最大化
- 過去記事の人気度を定量評価し、次のコンテンツ戦略に反映

### ワークフロー構成

```
[1] Schedule Trigger（水・土 09:00）
     ↓
[2] Notion Query: note記事管理DB（過去30日）
     ↓ Database ID: 29968d5c-2986-81ad-90d0-c24ed710503e
     ↓ Filter: Status = "検知済み" OR "分析済み"
[3] Function: スコアリング
     ↓ Score = (View Count × 0.6) + (Like Count × 0.4)
[4] Function: トップ10記事抽出
     ↓ スコア上位10件
[5] GPT-4 API: トピック抽出
     ↓ 共通キーワード、トレンド分析
[6] Notion: トピックマスタ更新
     ↓ Relation設定で関連記事リンク
[7] Slack: トピック通知
     ↓ 「今週のトレンドトピック: {トピック}」
```

### ノード詳細設計

#### Node 1: Schedule Trigger

```yaml
Node Type: Schedule Trigger
Trigger Interval: Custom
Cron Expression: 0 9 * * 3,6
  # 毎週水曜・土曜 09:00 JST
Timezone: Asia/Tokyo
```

---

#### Node 2: Notion Query - note記事管理DB（過去30日）

**変更点**: Google Sheets削除、Notion APIのみ使用

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.notion.com/v1/databases/29968d5c-2986-81ad-90d0-c24ed710503e/query
Authentication: None (Headerで指定)
Headers:
  - Authorization: Bearer YOUR_NOTION_API_TOKEN
  - Notion-Version: 2022-06-28
  - Content-Type: application/json

Body (JSON):
{
  "filter": {
    "and": [
      {
        "or": [
          {
            "property": "Status",
            "select": {"equals": "検知済み"}
          },
          {
            "property": "Status",
            "select": {"equals": "分析済み"}
          }
        ]
      },
      {
        "property": "Published Date",
        "date": {
          "after": "={{$now.minus({days: 30}).toISO()}}"
        }
      }
    ]
  },
  "sorts": [
    {"property": "View Count", "direction": "descending"}
  ],
  "page_size": 100
}
```

**レスポンス例**:
```json
{
  "results": [
    {
      "id": "29968d5c-2986-81e3-957b-e70b6507991e",
      "properties": {
        "Title": {
          "title": [{"text": {"content": "記事タイトル"}}]
        },
        "URL": {"url": "https://note.com/61beef/n/xxxxx"},
        "Article ID": {
          "rich_text": [{"text": {"content": "記事ID"}}]
        },
        "Published Date": {
          "date": {"start": "2025-10-27T12:13:00.000Z"}
        },
        "View Count": {"number": 1234},
        "Like Count": {"number": 56},
        "Description": {
          "rich_text": [{"text": {"content": "記事概要..."}}]
        },
        "Category": {
          "multi_select": [
            {"name": "MEO"},
            {"name": "マーケティング"}
          ]
        }
      }
    }
  ]
}
```

---

#### Node 3: Function - スコアリング

**変更点**: Notion APIレスポンス構造に対応

```javascript
// Notion APIレスポンスからデータ抽出
const notionResponse = $input.first().json;
const articles = notionResponse.results || [];

const scoredArticles = articles.map(page => {
  const props = page.properties;

  // プロパティから値を抽出（Notion API形式）
  const title = props.Title?.title[0]?.text?.content || '';
  const url = props.URL?.url || '';
  const articleId = props['Article ID']?.rich_text[0]?.text?.content || '';
  const viewCount = props['View Count']?.number || 0;
  const likeCount = props['Like Count']?.number || 0;
  const description = props.Description?.rich_text[0]?.text?.content || '';
  const categories = props.Category?.multi_select?.map(c => c.name) || [];
  const publishedDate = props['Published Date']?.date?.start || '';

  // スコア計算: PV×0.6 + いいね×0.4
  const score = (viewCount * 0.6) + (likeCount * 0.4);

  return {
    pageId: page.id,
    title,
    url,
    articleId,
    viewCount,
    likeCount,
    score,
    description,
    categories,
    publishedDate
  };
});

// スコア降順でソート
scoredArticles.sort((a, b) => b.score - a.score);

return scoredArticles.map(article => ({ json: article }));
```

---

#### Node 4: Function - トップ10記事抽出

```javascript
const articles = $input.all().map(item => item.json);

// スコア上位10件を抽出
const top10 = articles.slice(0, 10);

return [{ json: { articles: top10 } }];
```

---

#### Node 5: GPT-4 API - トピック抽出

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.openai.com/v1/chat/completions
Authentication: Bearer Token
Headers:
  - Authorization: Bearer {{GPT4_API_KEY}}
  - Content-Type: application/json

Body (JSON):
{
  "model": "gpt-4-turbo-preview",
  "messages": [
    {
      "role": "system",
      "content": "あなたはコンテンツストラテジストです。note記事データから、トレンドトピックを抽出してください。"
    },
    {
      "role": "user",
      "content": "以下の人気note記事データから、トレンドトピックを抽出してください。\n\n【データ】\n={{JSON.stringify($json.articles, null, 2)}}\n\n【分析観点】\n1. 共通キーワードの抽出\n2. ユーザーニーズの推定\n3. 次に書くべきトピック提案（5つ、優先度順）\n\n【出力形式】\nJSON形式で以下のように出力:\n{\n  \"trendKeywords\": [\"キーワード1\", \"キーワード2\", ...],\n  \"userNeeds\": \"ニーズ分析\",\n  \"nextTopics\": [\n    {\"topic\": \"トピック名\", \"priority\": 1, \"reason\": \"理由\"},\n    ...\n  ]\n}"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 1500
}
```

**レスポンス処理（Function Node追加）**:
```javascript
const gptResponse = $input.first().json;
const content = gptResponse.choices[0].message.content;

// JSONパース
let analysis;
try {
  analysis = JSON.parse(content);
} catch (e) {
  // パースエラー時はSlack通知
  throw new Error(`GPT-4レスポンスのJSON parse失敗: ${content}`);
}

return [{ json: analysis }];
```

---

#### Node 6: Notion - トピックマスタ更新

**変更点**: Relation設定でnote記事管理DBとリンク

```yaml
Node Type: HTTP Request (Loop)
Method: POST
URL: https://api.notion.com/v1/pages
Headers:
  - Authorization: Bearer YOUR_NOTION_API_TOKEN
  - Notion-Version: 2022-06-28
  - Content-Type: application/json

Body (JSON):
{
  "parent": {
    "database_id": "{{TOPIC_MASTER_DB_ID}}"
  },
  "properties": {
    "Topic Name": {
      "title": [
        {"text": {"content": "={{$json.topic}}"}}
      ]
    },
    "Score": {
      "number": "={{$json.avgScore}}"
    },
    "Keywords": {
      "multi_select": "={{$json.trendKeywords.map(k => ({name: k}))}}"
    },
    "Related Articles": {
      "relation": "={{$json.relatedArticleIds.map(id => ({id: id}))}}"
    },
    "Extracted At": {
      "date": {"start": "={{$now.toISO()}}"}
    },
    "Status": {
      "select": {"name": "active"}
    }
  }
}
```

**前処理（Function Node）**:
```javascript
const analysis = $input.first().json;
const top10Articles = $('Function: トップ10記事抽出').first().json.articles;

// 各トピックに関連記事を紐付け
const topics = analysis.nextTopics.map(topic => {
  // トピックキーワードに関連する記事を抽出
  const relatedArticles = top10Articles.filter(article => {
    const matchKeyword = analysis.trendKeywords.some(keyword =>
      article.title.includes(keyword) ||
      article.description.includes(keyword) ||
      article.categories.includes(keyword)
    );
    return matchKeyword;
  });

  // 関連記事のNotion Page IDを取得
  const relatedArticleIds = relatedArticles.map(a => a.pageId);

  // 平均スコア計算
  const avgScore = relatedArticles.length > 0
    ? relatedArticles.reduce((sum, a) => sum + a.score, 0) / relatedArticles.length
    : 0;

  return {
    topic: topic.topic,
    priority: topic.priority,
    reason: topic.reason,
    trendKeywords: analysis.trendKeywords,
    relatedArticleIds,
    avgScore
  };
});

return topics.map(t => ({ json: t }));
```

---

#### Node 7: Slack - トピック通知

```yaml
Node Type: HTTP Request
Method: POST
URL: {{SLACK_WEBHOOK_URL}}
Headers:
  - Content-Type: application/json

Body (JSON):
{
  "text": "📊 *今週のトレンドトピック分析完了*",
  "blocks": [
    {
      "type": "header",
      "text": {
        "type": "plain_text",
        "text": "🔥 トレンドトピック Top 5"
      }
    },
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "={{$input.all().map((t, i) => `${i+1}. *${t.json.topic}*\nスコア: ${t.json.avgScore.toFixed(1)} | 関連記事: ${t.json.relatedArticleIds.length}件\n理由: ${t.json.reason}`).join('\n\n')}}"
      }
    },
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": "*トレンドキーワード*: ={{$('Function: GPT-4レスポンス処理').first().json.trendKeywords.join(', ')}}"
      }
    }
  ]
}
```

---

### エラーハンドリング

```yaml
Notion API失敗:
  - Retry: 3回（指数バックオフ）
  - Fallback: Slack警告通知
  - 重大度: High

GPT-4 API失敗:
  - Retry: 2回
  - Fallback: 前回のトピックデータ使用 + Slack通知
  - 重大度: Medium

JSON Parse失敗:
  - Retry: なし
  - Fallback: Slack緊急通知 + 手動確認
  - 重大度: High
```

---

## WF6: note記事自動生成

### 基本情報

- **工数**: 30🍅（10ユニット）
- **目的**: トピックマスタに基づき、GPT-4でnote記事を自動生成・公開
- **トリガー**: Schedule（毎日 10:00）
- **実行頻度**: 毎日1本 → 週7本（目標10本達成のため手動追加実行も可）
- **データソース**: **Notion のみ**（Google Sheets不使用）

### 設計意図

**なぜこのワークフローが必要か**:
- 手動記事作成は時間がかかる（1本2-3時間）
- AI自動化で週10本のペースを維持
- 一定品質を保ちながら量産体制を構築

### ワークフロー構成

```
[1] Schedule Trigger（毎日 10:00）
     ↓
[2] Notion Query: トピックマスタ取得
     ↓ Status='active' のトピック
[3] Function: トピック選択（スコア最高）
     ↓
[4] GPT-4 API: 記事タイトル生成（3候補）
     ↓ 疑問形、How-to、ストーリー形式
[5] Function: タイトル選択（最もCTR高そう）
     ↓
[6] GPT-4 API: 記事本文生成（3,000-5,000文字）
     ↓ 構成: 導入→本論→結論
[7] Function: note Markdown変換
     ↓ HTML → note Markdown形式
[8] HTTP Request: note API投稿
     ↓ タイトル、本文、カテゴリ、タグ
[9] Notion: note記事管理DB登録
     ↓ Generated By = "AI"
[10] Slack: 投稿完了通知
     ↓ 「新規記事公開: {タイトル}」
```

### ノード詳細設計

#### Node 1: Schedule Trigger

```yaml
Node Type: Schedule Trigger
Trigger Interval: Every Day
Hour: 10
Minute: 0
Timezone: Asia/Tokyo
```

---

#### Node 2: Notion Query - トピックマスタ取得

**変更点**: Notion APIでトピックマスタを取得

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.notion.com/v1/databases/{{TOPIC_MASTER_DB_ID}}/query
Headers:
  - Authorization: Bearer YOUR_NOTION_API_TOKEN
  - Notion-Version: 2022-06-28
  - Content-Type: application/json

Body (JSON):
{
  "filter": {
    "property": "Status",
    "select": {"equals": "active"}
  },
  "sorts": [
    {"property": "Score", "direction": "descending"}
  ],
  "page_size": 10
}
```

---

#### Node 3: Function - トピック選択（スコア最高）

```javascript
const topicsResponse = $input.first().json;
const topics = topicsResponse.results || [];

if (topics.length === 0) {
  throw new Error('アクティブなトピックが存在しません。WF5を実行してください。');
}

// スコア最高のトピックを選択
const selectedTopic = topics[0];
const props = selectedTopic.properties;

const topicData = {
  topicId: selectedTopic.id,
  topicName: props['Topic Name']?.title[0]?.text?.content || '',
  score: props.Score?.number || 0,
  keywords: props.Keywords?.multi_select?.map(k => k.name) || [],
  relatedArticleIds: props['Related Articles']?.relation?.map(r => r.id) || []
};

return [{ json: topicData }];
```

---

#### Node 4: GPT-4 API - 記事タイトル生成（3候補）

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.openai.com/v1/chat/completions
Headers:
  - Authorization: Bearer {{GPT4_API_KEY}}
  - Content-Type: application/json

Body (JSON):
{
  "model": "gpt-4-turbo-preview",
  "messages": [
    {
      "role": "system",
      "content": "あなたはnoteで人気の記事タイトルを書く専門家です。"
    },
    {
      "role": "user",
      "content": "【トピック】\n={{$json.topicName}}\n\n【関連キーワード】\n={{$json.keywords.join(', ')}}\n\n【タスク】\n以下の3つのタイトルを生成してください:\n1. 疑問形（例: 「〜は本当に効果的？」）\n2. How-to形式（例: 「〜を成功させる5つの方法」）\n3. ストーリー形式（例: 「〜で失敗した私が学んだこと」）\n\n【制約】\n- 各30文字以内\n- 数字を含める\n- 検索されやすいキーワードを含む\n\n【出力形式】\nJSON形式で以下のように出力:\n{\n  \"titles\": [\n    {\"type\": \"question\", \"title\": \"タイトル1\"},\n    {\"type\": \"howto\", \"title\": \"タイトル2\"},\n    {\"type\": \"story\", \"title\": \"タイトル3\"}\n  ]\n}"
    }
  ],
  "temperature": 0.8,
  "max_tokens": 500
}
```

**レスポンス処理（Function Node）**:
```javascript
const gptResponse = $input.first().json;
const content = gptResponse.choices[0].message.content;

let titlesData;
try {
  titlesData = JSON.parse(content);
} catch (e) {
  throw new Error(`タイトル生成のJSON parse失敗: ${content}`);
}

return [{ json: titlesData }];
```

---

#### Node 5: Function - タイトル選択（最もCTR高そう）

```javascript
const titlesData = $input.first().json;
const titles = titlesData.titles || [];

// 簡易的なCTR予測（実際にはA/Bテストデータから学習すべき）
// ここでは how-to形式 > 疑問形 > ストーリー形式 の優先度とする
const priorityOrder = { 'howto': 3, 'question': 2, 'story': 1 };

titles.sort((a, b) => {
  return (priorityOrder[b.type] || 0) - (priorityOrder[a.type] || 0);
});

const selectedTitle = titles[0].title;

// トピックデータを引き継ぎ
const topicData = $('Function: トピック選択').first().json;

return [{
  json: {
    ...topicData,
    selectedTitle,
    allTitles: titles
  }
}];
```

---

#### Node 6: GPT-4 API - 記事本文生成（3,000-5,000文字）

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.openai.com/v1/chat/completions
Headers:
  - Authorization: Bearer {{GPT4_API_KEY}}
  - Content-Type: application/json

Body (JSON):
{
  "model": "gpt-4-turbo-preview",
  "messages": [
    {
      "role": "system",
      "content": "あなたは英語学童・バイリンガル教育の専門ライターです。"
    },
    {
      "role": "user",
      "content": "【タイトル】\n={{$json.selectedTitle}}\n\n【トピック】\n={{$json.topicName}}\n\n【関連キーワード】\n={{$json.keywords.join(', ')}}\n\n【ターゲット読者】\n- 3-10歳の子供を持つ保護者\n- 英語教育に関心がある\n- 教室選びを検討中\n\n【構成】\n1. 導入（500文字）: 読者の悩みに共感\n2. 本論（2,500文字）: 具体的な解決策・事例\n3. 結論（500文字）: アクション呼びかけ\n\n【トーン】\n- 親しみやすい\n- 専門的すぎない\n- 具体例を多用\n\n【制約】\n- 合計3,000-5,000文字\n- 見出しは ## を使用\n- 箇条書きは - を使用\n\n【出力形式】\nnote Markdown形式で出力してください。"
    }
  ],
  "temperature": 0.7,
  "max_tokens": 4000
}
```

**レスポンス処理（Function Node）**:
```javascript
const gptResponse = $input.first().json;
const markdownBody = gptResponse.choices[0].message.content;

// トピックデータとタイトルを引き継ぎ
const prevData = $input.first().json;

return [{
  json: {
    ...prevData,
    markdownBody,
    bodyLength: markdownBody.length
  }
}];
```

---

#### Node 7: Function - note Markdown変換

```javascript
// 既にnote Markdown形式なので、特別な変換は不要
// 必要に応じてフォーマット調整

const data = $input.first().json;
let markdown = data.markdownBody;

// noteの制約に合わせて調整
// 例: 画像埋め込み、リンク整形など

// カテゴリ・タグ生成
const categories = data.keywords.slice(0, 3); // 上位3キーワードをカテゴリに

return [{
  json: {
    title: data.selectedTitle,
    body: markdown,
    categories,
    topicName: data.topicName,
    topicId: data.topicId
  }
}];
```

---

#### Node 8: HTTP Request - note API投稿

**注意**: note公式APIは存在しないため、実装方法は以下のいずれか:
1. note非公式API（リバースエンジニアリング、非推奨）
2. note GUI自動化（Playwright等）
3. **推奨**: note Chrome Extension + Webhook連携

**ここでは概念設計のみ記載**:

```yaml
Node Type: HTTP Request
Method: POST
URL: https://note.com/api/v2/notes
  # 注: 公式APIが存在しないため、実際のエンドポイントは異なる
Headers:
  - Authorization: Bearer {{NOTE_API_TOKEN}}
  - Content-Type: application/json

Body (JSON):
{
  "note": {
    "name": "={{$json.title}}",
    "body": "={{$json.body}}",
    "status": "published",
    "price": 0,
    "hashtags": "={{$json.categories.map(c => `#${c}`).join(' ')}}"
  }
}
```

**代替案（推奨）**: Playwright自動化
- n8n + Playwright MCPでnote GUIを自動操作
- より安定した投稿が可能

**レスポンス処理（Function Node）**:
```javascript
const noteResponse = $input.first().json;

// note投稿成功後、記事URLとIDを取得
const noteUrl = noteResponse.data?.url || '';
const articleId = noteUrl.split('/').pop() || '';

const prevData = $('Function: note Markdown変換').first().json;

return [{
  json: {
    ...prevData,
    noteUrl,
    articleId,
    publishedAt: new Date().toISOString()
  }
}];
```

---

#### Node 9: Notion - note記事管理DB登録

**重要な変更**: `Generated By = "AI"` を必ず設定

```yaml
Node Type: HTTP Request
Method: POST
URL: https://api.notion.com/v1/pages
Headers:
  - Authorization: Bearer YOUR_NOTION_API_TOKEN
  - Notion-Version: 2022-06-28
  - Content-Type: application/json

Body (JSON):
{
  "parent": {
    "database_id": "29968d5c-2986-81ad-90d0-c24ed710503e"
  },
  "properties": {
    "Title": {
      "title": [{"text": {"content": "={{$json.title}}"}}]
    },
    "URL": {
      "url": "={{$json.noteUrl}}"
    },
    "Article ID": {
      "rich_text": [{"text": {"content": "={{$json.articleId}}"}}]
    },
    "Published Date": {
      "date": {"start": "={{$json.publishedAt}}"}
    },
    "View Count": {
      "number": 0
    },
    "Like Count": {
      "number": 0
    },
    "Status": {
      "select": {"name": "検知済み"}
    },
    "Generated By": {
      "select": {"name": "AI"}
    },
    "Description": {
      "rich_text": [{"text": {"content": "={{$json.body.slice(0, 200)}}"}}]
    },
    "Image URL": {
      "url": ""
    },
    "Author": {
      "rich_text": [{"text": {"content": "61beef"}}]
    },
    "Category": {
      "multi_select": "={{$json.categories.map(c => ({name: c}))}}"
    }
  }
}
```

---

#### Node 10: Slack - 投稿完了通知

```yaml
Node Type: HTTP Request
Method: POST
URL: {{SLACK_WEBHOOK_URL}}
Headers:
  - Content-Type: application/json

Body (JSON):
{
  "text": "📝 *新規note記事公開*",
  "blocks": [
    {
      "type": "header",
      "text": {
        "type": "plain_text",
        "text": "✨ AI生成記事が公開されました"
      }
    },
    {
      "type": "section",
      "fields": [
        {
          "type": "mrkdwn",
          "text": "*タイトル*\n={{$json.title}}"
        },
        {
          "type": "mrkdwn",
          "text": "*トピック*\n={{$json.topicName}}"
        },
        {
          "type": "mrkdwn",
          "text": "*文字数*\n={{$json.bodyLength}}文字"
        },
        {
          "type": "mrkdwn",
          "text": "*カテゴリ*\n={{$json.categories.join(', ')}}"
        }
      ]
    },
    {
      "type": "actions",
      "elements": [
        {
          "type": "button",
          "text": {"type": "plain_text", "text": "記事を見る"},
          "url": "={{$json.noteUrl}}"
        }
      ]
    }
  ]
}
```

---

### エラーハンドリング

```yaml
Notion API失敗（トピック取得）:
  - Retry: 3回
  - Fallback: 前回使用したトピックで再生成 + Slack通知
  - 重大度: Medium

GPT-4 API失敗（タイトル生成）:
  - Retry: 2回
  - Fallback: テンプレートタイトル使用 + Slack通知
  - 重大度: Medium

GPT-4 API失敗（本文生成）:
  - Retry: 2回
  - Fallback: 下書き保存 + 手動編集依頼Slack通知
  - 重大度: High

note API失敗（投稿）:
  - Retry: 3回
  - Fallback: 記事データをNotion保存 + 手動投稿依頼Slack通知
  - 重大度: High

Notion API失敗（登録）:
  - Retry: 3回
  - Fallback: Slack緊急通知 + 記事データをログ保存
  - 重大度: Critical
```

---

## WF7, WF8, WF9 の詳細設計

**注**: WF7-9も同様にNotion単独管理で設計します。詳細は必要に応じて追加実装します。

### WF7: SNS動画化

**データソース**: Notion note記事管理DB のみ
**動画保存先**: Google Drive → Notion 動画管理DB に URL保存
**変更点**: Google Sheets削除

### WF8: SNS投稿自動化

**データソース**: Notion note記事管理DB のみ
**投稿ログ**: Notion SNS投稿ログDB
**変更点**: Google Sheets削除、今日の記事取得はNotion Queryで実施

### WF9: LINE再配信

**データソース**: Notion note記事管理DB（人気記事）+ Notion LINE Lead CRM DB（友だちリスト）
**変更点**: Google Sheets完全削除、Notion Queryのみ使用

---

## 技術要件

### API統合

| API | 用途 | 認証方式 | レート制限 | コスト |
|-----|------|----------|-----------|--------|
| **Notion API** | データ管理 | Bearer Token | 3 req/sec | 無料 |
| **GPT-4 API** | 記事生成、トピック抽出 | API Key | 10,000 req/日 | $0.03/1K tokens |
| **note API** | 記事投稿 | OAuth2/非公式 | 不明 | 無料 |
| **X API v2** | ツイート投稿 | OAuth 2.0 | 1,500 req/日 | 無料（Free Tier） |
| **Instagram Graph API** | 投稿 | Access Token | 200 req/時 | 無料 |
| **Runway API** | 動画生成 | API Key | クレジット制 | $12/100クレジット |
| **Google Drive API** | 動画保存 | OAuth2 | 1,000 req/日 | 無料 |

### コスト見積もり

**月次コスト**:
- GPT-4 API: $50（記事生成10本/日×30日、1記事5,000 tokens）
- Runway: $36（動画10本/日×30日、1動画1.2クレジット）
- note API: 無料
- X API: 無料（Free Tier）
- Instagram API: 無料
- Notion API: 無料
- **Google Sheets API**: $0（不使用）

**合計**: 約$86/月（約¥13,000）

**Phase1からの変更**: Google Sheets API費用$0削減（レート制限管理コスト削減）

---

## テスト計画

### Phase2統合テスト

#### テストケース1: WF5 → WF6 連携

```
シナリオ: トピック抽出 → 記事自動生成

1. WF4でnote記事データ蓄積（Phase1完了）
2. WF5実行 → トピックマスタ更新
3. WF6実行 → AI記事生成・note投稿
4. Notion note記事管理DBに登録確認
5. `Generated By = "AI"` 確認
6. Slack通知確認

期待結果:
- トピックマスタに5件のトピック登録
- note記事1件公開
- Notion note記事管理DBに1件追加（Generated By = "AI"）
- Slack通知送信
```

#### テストケース2: Notion単独管理の整合性

```
シナリオ: Notionデータの一貫性確認

1. WF5でトピック抽出
2. トピックマスタのRelation確認（note記事管理DBへのリンク）
3. WF6で記事生成
4. note記事管理DBの全プロパティ確認
5. 手動生成記事とAI生成記事の区別確認

期待結果:
- Relation正常動作
- プロパティ名統一（Description, Image URL, Author, Category）
- `Generated By` で手動/AI区別可能
- Google Sheetsへのアクセスなし
```

#### テストケース3: エラーハンドリング

```
シナリオ: Notion API障害時の動作確認

1. Notion APIをモック障害
2. WF5実行
3. リトライ3回確認
4. Slack警告通知確認

期待結果:
- 3回リトライ実行
- Slack警告通知送信
- エラーログ記録
```

---

## Phase2完了判定基準

### 必須要件

- [ ] WF5, WF6, WF7, WF8, WF9すべて稼働
- [ ] note記事40本公開達成（手動+AI）
- [ ] SNS投稿40本達成
- [ ] 累計PV 22,000達成
- [ ] **Notionデータの整合性 >99%**（Google Sheets不使用）
- [ ] AI生成記事と手動記事の区別が明確

### 品質基準

- [ ] AI記事生成成功率 >90%
- [ ] 動画生成成功率 >80%
- [ ] SNS投稿成功率 >95%
- [ ] LINE再配信開封率 >40%
- [ ] エラー率 <1%

### オプション要件

- [ ] トピック抽出精度の向上（A/Bテスト）
- [ ] 記事品質の手動レビュー（週1回）

---

## 関連ドキュメント

### Phase1関連
- [[Phase1_実装変更点サマリー_Phase2への影響分析]] - Phase1変更点詳細
- [[Phase1_n8nワークフロー詳細設計書]] - Phase1設計書
- [[WF4_実装完了レポート_20251027]] - WF4実装詳細

### Phase2関連
- [[MEO集客自動化_n8nワークフロー全体構成]] - 全体アーキテクチャ
- [[Phase2_n8nワークフロー詳細設計書]] - 前バージョン（v1.0）

### 次フェーズ
- [[Phase3_n8nワークフロー詳細設計書]] - CVR最適化
- [[Phase4_n8nワークフロー詳細設計書]] - AI PDCA

---

## 更新履歴

| バージョン | 日付 | 変更内容 | 作成者 |
|-----------|------|----------|--------|
| 1.0 | 2025-10-27 | 初版作成 - Phase2全ワークフロー設計 | Claude |
| **2.0** | **2025-10-27** | **Phase1実装変更を完全反映 - Notion単独管理ベースで全面改訂** | **Claude Code** |

**主要な変更点**:
- Google Sheets API呼び出しを全削除
- Notion API のみに統一
- Database ID明示（`29968d5c-2986-81ad-90d0-c24ed710503e`）
- プロパティ名統一（Description, Image URL, Generated By, Author）
- WF5-9の全ノード設計を詳細化

---

**次のステップ**: Phase1完了後、WF5から順次実装

**推奨**: トピックマスタDB, 動画管理DB, SNS投稿ログDB を事前作成
