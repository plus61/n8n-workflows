# n8n-MCP URL接続問題 - 完全調査記録

**日時**: 2025-11-03 15:31:38 - 15:55
**状態**: 解決済み - Claude Code再起動待ち

---

## 問題の概要

n8n-mcpが本来接続すべきURL（`https://n8n-python-production-344b.up.railway.app`）ではなく、
古いURL（`https://n8n-python-production-344b.up.railway.app`）に接続している問題。

---

## 根本原因の特定

### ✅ 設定ファイルの確認

**ファイル**: `~/Library/Application Support/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "env": {
        "N8N_API_URL": "https://n8n-python-production-344b.up.railway.app/",
        "N8N_API_KEY": "eyJ..."
      }
    }
  }
}
```

**重要**: Claude CodeとClaude Desktopの両方がこの設定ファイルを共有しています。

### ❌ 実際の接続状態

**確認コマンド**: `mcp__n8n-mcp__n8n_health_check`

```json
{
  "apiUrl": "https://n8n-python-production-344b.up.railway.app",
  "mcpVersion": "2.18.6"
}
```

### 🔍 原因分析

1. **プロセスキャッシュ問題**
   - 10個のn8n-mcpプロセスが実行中
   - 既存プロセスが古い環境変数をキャッシュ
   - 設定ファイル更新後も反映されない

2. **バージョン混在**
   - 診断結果: `v2.18.6`
   - Homebrewインストール: `v2.22.6`
   - npxキャッシュ: `v2.22.8`

3. **URL末尾のスラッシュ**
   - 設定: `https://...app/`（スラッシュあり）
   - 実際の接続: スラッシュなしで処理される可能性

---

## 実施した対応

### ステップ1: プロセス停止 ✅

```bash
pkill -f "n8n-mcp"
```

実行中のすべてのn8n-mcpプロセスを停止しました。

### ステップ2: 設定修正 ✅

Claude Desktop configのURL末尾のスラッシュを削除：

```diff
- "N8N_API_URL": "https://n8n-python-production-344b.up.railway.app/",
+ "N8N_API_URL": "https://n8n-python-production-344b.up.railway.app",
```

### ステップ3: キャッシュクリア ✅

```bash
rm -rf ~/.npm/_npx/*/node_modules/n8n-mcp
```

npxキャッシュから古いn8n-mcpバージョンを削除しました。

---

## Claude Code再起動手順

### 重要な理解

- **Claude Code** = CLIツール（会話履歴なし）
- **Claude Desktop** = デスクトップアプリ（会話履歴あり）
- 両者は同じMCP設定ファイルを共有

### 再起動方法

1. **このチャットを終了**
   ```bash
   exit
   ```
   または `Ctrl+C`

2. **新しいセッションを開始**
   ```bash
   claude
   ```

3. **接続確認（新しいセッション内）**
   ```
   mcp__n8n-mcp__n8n_health_check
   ```

### 期待される結果

```json
{
  "status": "ok",
  "apiUrl": "https://n8n-python-production-344b.up.railway.app",
  "mcpVersion": "2.22.x"
}
```

---

## 確認事項チェックリスト

### ✅ 接続確認

```bash
# 1. ヘルスチェック
mcp__n8n-mcp__n8n_health_check

# 期待値:
# - apiUrl: https://n8n-python-production-344b.up.railway.app
# - status: ok
```

### ✅ 機能テスト

```bash
# 2. ワークフローリスト取得
mcp__n8n-mcp__n8n_list_workflows --limit 5

# 成功すれば接続完了
```

### ✅ バージョン確認

```bash
# 3. MCPバージョン確認（health_checkに含まれる）
# 期待値: 2.22.x 以上
```

---

## トラブルシューティング

### 問題: 再起動後も古いURLに接続

**対処法**:

1. Claude Desktopも同時に再起動
   ```bash
   # Claude Desktopを完全終了 → 再起動
   ```

2. 設定ファイル再確認
   ```bash
   grep "N8N_API_URL" ~/Library/Application\ Support/Claude/claude_desktop_config.json

   # 出力例: "N8N_API_URL": "https://n8n-python-production-344b.up.railway.app",
   ```

3. プロセス確認
   ```bash
   ps aux | grep n8n-mcp

   # 複数プロセスがある場合は再度停止
   ```

### 問題: MCPツールが利用できない

**対処法**:

1. MCP設定の確認
   ```bash
   cat ~/Library/Application\ Support/Claude/claude_desktop_config.json | grep -A 5 "n8n-mcp"
   ```

2. n8n-mcpインストール確認
   ```bash
   which n8n-mcp
   # 出力: /opt/homebrew/bin/n8n-mcp
   ```

3. 手動起動テスト
   ```bash
   N8N_API_URL="https://n8n-python-production-344b.up.railway.app" \
   N8N_API_KEY="eyJ..." \
   n8n-mcp
   ```

---

## 参考情報

### 環境情報

- **OS**: macOS Darwin 24.6.0
- **Node.js**: v24.1.0
- **n8n-mcp**: v2.22.6 (Homebrew), v2.22.8 (npx cache)
- **Claude Code**: CLI version

### 関連ファイルパス

```
~/Library/Application Support/Claude/
├── claude_desktop_config.json    # MCP設定（Claude Code & Desktop共有）
└── ...

~/Library/Logs/Claude/
├── mcp-server-n8n-mcp.log       # n8n-mcpログ
└── ...

/opt/homebrew/Cellar/node/24.1.0/
└── lib/node_modules/n8n-mcp/    # Homebrewインストール
```

### Railway URL履歴

- ✅ **正しいURL**: `https://n8n-python-production-344b.up.railway.app`
- ❌ **古いURL**: `https://n8n-python-production-344b.up.railway.app`

---

## タスク進捗

- [x] 問題の根本原因を特定
- [x] すべてのn8n-mcpプロセスを停止
- [x] Claude Desktop configを修正（末尾スラッシュ削除）
- [x] npxキャッシュをクリア
- [x] 調査結果を文書化
- [ ] Claude Code再起動
- [ ] 新しいセッションで接続確認

---

## 次のアクション

1. **このチャットを終了**
2. **新しいClaude Codeセッションを開始**
3. **上記の確認事項を実施**

**成功基準**: `apiUrl`が`https://n8n-python-production-344b.up.railway.app`になっている

---

**調査担当**: Claude Code (AI Assistant)
**最終更新**: 2025-11-03 15:55
